import { Placeholder } from "@/components/placeholder";

export default function Dashboard() {
  return (
    <Placeholder
      step="Step 3"
      title="Recruiter Dashboard"
      description="Open host-startup roles and live cycle stats will render here — reading from the seeded data layer built in Step 2."
    />
  );
}
