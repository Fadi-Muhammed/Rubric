import { Placeholder } from "@/components/placeholder";

export default function Allocation() {
  return (
    <Placeholder
      step="Step 9 — headline"
      title="Allocation Board"
      description="One-click assignment of the whole pool across every host startup — startups as columns, contested candidates highlighted, drag to override."
    />
  );
}
