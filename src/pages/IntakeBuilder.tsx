import { Placeholder } from "@/components/placeholder";

export default function IntakeBuilder() {
  return (
    <Placeholder
      step="Step 7"
      title="Intake Builder"
      description="The 3-step flow (details → form/question builder → publish + shareable link) with the AI writing assistant lands here."
    />
  );
}
